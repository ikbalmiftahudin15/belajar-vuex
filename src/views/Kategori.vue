<template>
    <br>
    <section class="bg-white py-12 text-gray-700 sm:py-16 lg:py-20">
  <div class="mx-auto max-w-screen-xl px-4 sm:px-6 lg:px-8">
    <div class="mx-auto max-w-md text-center">
      <h2 class="font-serif text-2xl font-bold sm:text-3xl">Category PRODUCT</h2>
    </div>

    <div class="mt-10 grid grid-cols-6 gap-6 lg:mt-10 lg:grid-cols-4 lg:gap-4">
        <div v-for="kategori in getKategori">

          
      <article class="relative">
        <div class="aspect-square overflow-hidden">
          <img class="group-hover:scale-125 h-full w-full object-cover transition-all duration-300" :src="kategori.image " alt="" />
        </div>
        <div class="mt-4 flex items-start justify-between">
          <div class="">
            <h3 class="text-xs font-semibold sm:text-sm md:text-base">
              <a href="#" title="" class="cursor-pointer">
                {{ kategori }}
                <span class="absolute" aria-hidden="true"></span>
              </a>
            </h3>
           </div>
        </div>
      </article>
      </div>
    </div>
  </div>
</section>

</template>
    
    <script>
    import { mapGetters, mapActions } from 'vuex';
    
    export default {
        computed: {
            ...mapGetters('kategori', ['getKategori']),
        },
        methods: {
            ...mapActions('kategori', ['fetchKategori']),
            getImage(imageURL) {
                return {
                    'src': `${imageURL}`,
                    'background-size': 'cover',
                    'background-position': 'center'
                }
            }
        },
        created() {
            this.fetchKategori();
        }
   }
</script>